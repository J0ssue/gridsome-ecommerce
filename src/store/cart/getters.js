export function cartProducts(state) {
  return state.cartProducts;
}

export function cartContent(state) {
  return state.content;
}

export function paginationData(state) {
  return state.pagination;
}